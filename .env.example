# 🌟 Ratings & Reviews POC - Environment Configuration
#
# This file contains environment variable examples for the entire monorepo.
# Copy this file and configure for your specific environment.

# ============================================================================
# BACKEND SERVICE CONFIGURATION
# ============================================================================
# For detailed backend configuration, see backend/.env.example
# All backend environment variables should be prefixed with their section

# --- Server Configuration ---
PORT=8080
NODE_ENV=development

# --- CommerceTools Configuration ---
# Get these credentials from CommerceTools Merchant Center
# See docs/COMMERCETOOLS_SETUP.md for detailed instructions

CTP_PROJECT_KEY=your-project-key
CTP_CLIENT_ID=your-client-id
CTP_CLIENT_SECRET=your-client-secret
CTP_API_URL=https://api.europe-west1.gcp.commercetools.com
CTP_AUTH_URL=https://auth.europe-west1.gcp.commercetools.com
CTP_SCOPES=manage_project:your-project-key,view_products:your-project-key

# --- JWT Configuration ---
# IMPORTANT: Change this in production to a strong random value (min 32 characters)
JWT_SECRET=change-this-secret-in-production-min-32-chars
JWT_EXPIRES_IN=24h

# --- Rate Limiting Configuration ---
# Window in milliseconds (60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000
# Maximum requests per window per IP
RATE_LIMIT_MAX_REQUESTS=10

# --- CORS Configuration ---
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,http://localhost:8080

# --- Logging Configuration ---
# Options: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# ============================================================================
# FRONTEND SERVICE CONFIGURATION
# ============================================================================
# Frontend typically doesn't need environment variables for local development
# In production, these are managed through the deployment process

# BACKEND_URL=http://localhost:8080

# ============================================================================
# INFRASTRUCTURE CONFIGURATION
# ============================================================================
# These are used by Terraform for GCP deployment
# See infra/README.md for detailed infrastructure documentation

# GCP_PROJECT_ID=your-gcp-project-id
# GCP_REGION=europe-west1
# TF_STATE_BUCKET=your-terraform-state-bucket

# ============================================================================
# NOTES
# ============================================================================
# 
# 1. NEVER commit this file with real credentials to version control
# 2. The .env file is already in .gitignore
# 3. For local development, copy this to .env and fill in your values
# 4. For CI/CD, use GitHub Secrets (see docs/COMMERCETOOLS_SETUP.md)
# 5. For production, use Google Secret Manager (see backend/docs/DEPLOYMENT.md)
#
# Quick Start:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your credentials
# 3. For backend only: cd backend && cp .env.example .env (recommended)
#
# Documentation:
# - CommerceTools Setup: docs/COMMERCETOOLS_SETUP.md
# - Backend Configuration: backend/.env.example
# - Deployment Guide: backend/docs/DEPLOYMENT.md
# - Infrastructure Guide: infra/README.md
